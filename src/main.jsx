import React from "react";
import ReactDOM from "react-dom/client";
import UniversalApp from "./UniversalApp.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UniversalApp />
  </React.StrictMode>,
);
