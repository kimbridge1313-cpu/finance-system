import admin from "firebase-admin";
function serviceAccount(){const raw=process.env.FIREBASE_SERVICE_ACCOUNT;if(!raw)throw new Error("Missing FIREBASE_SERVICE_ACCOUNT");return JSON.parse(raw);}function init(){if(admin.apps.length)return admin.app();return admin.initializeApp({credential:admin.credential.cert(serviceAccount())});}
export default async function handler(req,res){if(req.method!=="POST")return res.status(405).json({error:"Method not allowed"});try{const {lineUserId}=req.body||{};if(!lineUserId)return res.status(400).json({error:"Missing lineUserId"});init();const token=await admin.auth().createCustomToken(lineUserId,{provider:"line"});return res.status(200).json({token,uid:lineUserId});}catch(error){console.error("api/auth error:",error);return res.status(500).json({error:error.message||"Internal server error"});}}
